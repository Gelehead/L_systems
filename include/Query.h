#ifndef QUERY
#define QUERY 

// query class to interrogate the tiles 
// should be contained within the tile class
class Query {
    public :
        enum Type {
            neighbouring_tiles_type
        };
        int a;
};

#endif